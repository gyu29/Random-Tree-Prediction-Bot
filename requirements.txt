# Exact versions this project is developed and tested against. Pinned rather than
# floated for the same reason models/<category>/manifest.json records exact
# library_versions at training time (see README's Model Reproducibility and
# Integrity section): a silent version drift here is exactly the kind of mismatch
# that mechanism exists to catch, so this file shouldn't be the thing that causes it.
#
# Install with: pip install -r requirements.txt
pandas==2.0.3
numpy==1.26.4
scikit-learn==1.4.2
ta==0.11.0
joblib==1.3.2
requests==2.32.3
xgboost==3.2.0
yfinance==0.2.65
PySide6==6.11.1
pyqtgraph==0.14.0
defusedxml==0.7.1

# Only needed to run tests/ (pytest tests/)
pytest==8.3.5

# Only needed when building a standalone executable (see README's "Building a
# Standalone Python Executable")
pyinstaller==5.11.0
